#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void) /**to print a string using printf**/
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
