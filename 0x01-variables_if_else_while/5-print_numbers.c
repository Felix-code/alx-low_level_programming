#include <stdio.h>
/**
 * main - a program that prints all single digit of number in base 10
 *
 * Retult: Always 0 (Success)
 */

int main(void)
{
	int i = 0;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
